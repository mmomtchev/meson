#!/usr/bin/env python

import sys
import subprocess
import os

with open('@LOG@', 'a') as f:
    f.write(str(sys.argv))
    f.write('\n')

sys.exit(subprocess.run(['ninja', *sys.argv[1:]]).returncode)
